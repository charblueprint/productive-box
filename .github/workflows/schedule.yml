name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: worktime
        uses: charblueprint/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          GIST_ID: ${{ 538f30ebc66a5cd4bcec81a84154c0a7 }}
          TIMEZONE: Asia/Seoul
